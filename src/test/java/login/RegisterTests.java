package login;

import com.milbar.gui.configuration.ApplicationConfiguration;
import com.milbar.logic.login.UsersManager;
import com.milbar.logic.login.loaders.SerializedDataReader;
import com.milbar.logic.login.wrappers.UserCredentials;
import org.junit.Test;
import org.mockito.Mock;

import static org.mockito.ArgumentMatchers.anyString;
import static org.mockito.ArgumentMatchers.isA;
import static org.mockito.Mockito.when;

public class RegisterTests {
    
//    @Mock
//    SerializedDataReader<String, UserCredentials> usersCollection =
//            new SerializedDataReader<>(ApplicationConfiguration.getUsersDataPath());
//
//    UsersManager usersManager = new UsersManager(ApplicationConfiguration.getUsersDataPath());
//
//    @Test
//    public void WhenCreatingANewUser_NewCollectionOfUsersIsCreated() {
//        when(usersCollection.updateCollection(anyString(), isA(UserCredentials.class))).thenReturn(true);
//
//
//    }

}
